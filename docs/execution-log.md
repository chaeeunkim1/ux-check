# 실행 기록

## 2026-09-21 17:31 KST · 목표 등록

- 실제 goal 도구에 docs/goal-original.txt의 목표를 등록했으며 상태는 active이다.
- 준비된 기반: 공개 GitHub, Vercel 자동 배포, Claude Sonnet 4.6 실제 이미지 분석 확인.
- 시작 배포: a28bdec32198, https://ux-check-zeta.vercel.app.
- 예시로 받은 claude-sonnet-4-20250514는 API 404로 사용할 수 없어, Models API 목록에서 확인한 claude-sonnet-4-6으로 준비 단계에서 변경하고 실제 호출을 검증했다.
- 사용자 요청에 따라 기존 Claude 키를 유지하고 제출용 로그 사본에서만 비밀값을 마스킹한다.
- 첫 구현: 합성 화면 선택 → 업무 목적 → 실제 분석 → 근거와 개선안 → 보고서 내보내기. 이후 개선 전후 비교와 회귀 검증을 추가한다.
- 사용자 인증이 새로 필요한 항목은 내일 확인 목록에 남기되 나머지 개발을 계속한다.

## 첫 검수 흐름 구현 및 검증

- 합성 업무 화면 3종과 개선 전후 PNG 6장을 생성하고 승인 목록 이미지의 한글 렌더링을 눈으로 확인했다.
- 업로드 검증, 실제 Claude 도구 스키마 응답, 위치 표시, 우선순위 필터, Markdown/JSON 보고서, 전후 비교 UI를 구현했다.
- 최초 JSX 구문 오류를 빌드로 발견해 수정했고 프로덕션 빌드가 통과했다.
- 동의/입력, 위조 이미지, 스트리밍 용량 제한, 교차 출처 요청, 호출량·동시성, 잘못된 응답, 타임아웃, 보고서 내용에 대한 자동 테스트 10개가 통과했다.
- 승인 목록 실제 분석: HTTP 200, 약 56.4초, 입력 3,504 / 출력 3,497 토큰. 상태 색상 범례 부재와 불명확한 검색 안내 등을 관찰했다.
- 개선점 발견: 상세 화면의 후속 기능을 추측해 버튼 문제의 심각도를 높일 수 있어 프롬프트에 관찰 범위 제한을 강화했다. 최대 관찰 수를 5개로 줄여 설명 밀도와 응답 시간을 개선한다.
- 스크린샷 렌더링에서 작은 보조 문구가 흐려 보이는 것을 확인해 글자 크기와 대비를 보강했다.
- 공개 호출 제한은 서버 인스턴스별 메모리 기반이다. 전역 영구 과금 한도로 보장하지 않으며 현재는 인스턴스당 시간당 40회, 클라이언트당 10회, 동시 3회이다.

## 실제 예제 검증 확장 및 발견한 한계

- 점검 등록 단일 검수: HTTP 200, 약 45.2초, 입력 3,685 / 출력 2,832 토큰. 특정되지 않은 오류, 필수 표시 부재, 초기화 버튼의 우선 강조를 관찰했다.
- 대시보드 단일 검수: HTTP 200, 약 48.8초, 입력 3,679 / 출력 3,011 토큰. C1~C4 의미, 알림 맥락, 단위와 수치 표기 부재를 관찰했다.
- 승인 목록 전후 비교: HTTP 200, 약 59.7초, 입력 5,067 / 출력 3,378 토큰. 검색 안내와 날짜 열 개선은 찾았지만, 이미 텍스트 레이블이 있는 상태 배지에도 범례를 요구하는 과잉 판단이 있었다. 이 결과는 정확성 검증 통과로 처리하지 않는다.
- 수정: 색상과 텍스트가 함께 있는 상태를 미표시로 보지 않도록 하고, 여백·행 전체 색상·자연스러운 문장 등의 취향 차이를 결함으로 과장하지 않도록 검수 원칙을 보강했다. 비교 시 해결된 항목을 충분히 기록하고 새 문제를 억지로 채우지 않도록 했다.

## 공개 데모 실제 사용자 흐름

- b1aa76d 커밋의 GitHub Actions에서 자동 테스트와 프로덕션 빌드가 성공했다.
- 인증 없는 브라우저에서 공개 운영 페이지를 열고 AI 검수 시작을 눌렀다. 분석 대기 후 39.3초의 실제 결과가 표시됐다. 상태 설명 부재, 일자 맥락, 검색 안내, 필터 관찰 여부, 버튼 문구의 5개 항목과 근거·개선안·확인 기준이 표시됐다.
- 보고서 버튼을 누른 뒤 다운로드 요청 안내가 표시되었고 브라우저 오류 로그는 없었다. 다만 현재 자동화 브라우저에서 download 이벤트 대기가 시간 초과되어 실제 파일 저장은 아직 검증하지 못했다. 보고서 본문의 일치성은 자동 테스트에서 확인했다. 이 제한을 해결하거나 최종 확인 항목으로 남긴다.
- 보강한 비교 원칙으로 재실행: HTTP 200, 약 77.7초, 입력 5,490 / 출력 4,073 토큰. 텍스트 상태 배지 등 7개의 관찰 가능한 개선을 resolved로 분류했다. 하단 보조 설명에 대한 추가 지적 1건은 여전히 과잉 제안 가능성이 있어 최종 사용자 판단이 필요하다.
- 후속 개선: 담당자 채택·추가 확인·제외 상태를 원래 AI 근거와 분리해 보고서에 기록하도록 추가했다. 관련 테스트를 포함해 11개 테스트가 통과했다.
- 필터 상태에서 화면 번호를 눌렀을 때 해당 결과가 가려지는 경우를 예방하도록 전체 필터로 전환한다. 모바일에서는 분석 시작 후 결과 대기 영역으로 이동하도록 수정했다.

## 다음 검증 순서

1. 담당자 판단·필터 전환·보고서 미리보기/저장 대체 흐름을 공개 브라우저에서 확인한다.
2. 모바일/데스크톱, 키보드, 업로드·동의·오류·취소·재시도 흐름을 실행한다.
3. 비교의 근거를 다른 합성 예제에서도 검토하고 평가 기준과 한계를 정리한다.
4. 영상, 발표자료, 제출 문안, 공개 링크 검증과 최신 로그 내보내기를 준비한다.

## 보고서 전달 및 반응형 검증

- 실제 API 응답으로 만든 저장 예시 5개를 추가했다. 화면과 보고서에 "저장된 합성 예시 · 현재 실시간 분석 아님"을 표시하며 별도 버튼으로만 불러온다. 실시간 분석과 구분한다.
- 보고서 미리보기에서 사용자가 선택한 "개선안 채택"과 AI 관찰 근거가 함께 렌더링되는 것을 브라우저에서 확인했다.
- 임시 Blob 다운로드는 자동화 브라우저 이벤트가 확인되지 않았다. 보고서 스키마를 검증하고 HTTP Content-Disposition 첨부 파일로 반환하는 /api/export로 변경했다. 변경 후 실제 브라우저 download 이벤트가 확인됐다. 내용 일치, HTML 이스케이프, 형식 검증 테스트도 통과했다.
- 390px 모바일 뷰포트(스크롤바 제외 375px)에서 가로 넘침이 없음을 DOM 실측으로 확인했다. 잎 텍스트 요소 102개를 색상으로 측정해 낮은 대비를 발견했고 단계 번호, 상태 배지, 본문, 문제 번호 등의 색상을 보정했다. 이 부분 검사는 전체 접근성 인증이 아니다.
- 모바일에서 확대 보기 → 원본 크기 전환을 실행해 이미지의 한글을 읽을 수 있는 크기로 보고 스크롤할 수 있음을 확인했다. Escape 후 초점이 확대 보기 버튼으로 돌아왔다.
- 빈 업무 목적은 AI 호출 전에 차단됐다. 합성 예제를 다시 선택하면 오류가 해제되고 업무 목적이 복구됐다.
- 실제 파일 선택 흐름으로 합성 PNG를 업로드했다. 업로드 시 별도 외부 AI 전송 동의가 표시되며, 미동의 상태에서는 분석이 차단됐다.
- 추가 실제 비교: 점검 등록 폼에서 오류 위치, 필드 레이블, 필수 표시, 버튼 문구, 선택 메모 등 5개 개선을 확인했고 오류 표시 시점은 uncertain으로 분리했다. HTTP 200, 약 59.6초, 입력 5,497 / 출력 3,392 토큰.
- 승인 목록 재분석에서 일시적인 upstream_unavailable 오류 1회가 발생했다. 성공으로 기록하지 않으며 복구를 별도로 확인한다.

## AI 근거 재검토와 보고서 일치성

- 실제 단일 검수에서 MM.DD를 인용하면서 제목에는 월이 없다고 한 모순과, 비교에서 적절한 상태 텍스트에도 범례를 요구하는 과잉 제안을 발견했다. 문제 개수를 늘리는 대신 실제 이미지와 초안을 독립적인 두 번째 Claude 호출로 대조하도록 변경했다. 목표 변경은 아니며 근거 품질 개선이다.
- 두 번째 검토는 근거 없음 항목을 제외하고, 불확실 항목의 확신도를 낮추며, 제목과 우선순위를 조정한다. 실제 기능 검증이나 정확성 보증으로 표시하지 않는다. 두 호출의 입력·출력 토큰을 합산한다.
- 첫 전후 비교 재실행은 invalid_response HTTP 502로 실패했다. 잘못된 결과를 표시하지 않았다. 긴 구조화 응답의 출력 한도를 6,000으로 확대하고 잘림 오류를 별도로 구분했다. 실패의 정확한 원인이 잘림인지까지는 이전 응답을 보관하지 않아 확정하지 않았다.
- 이후 실제 전후 비교는 HTTP 200 / 81.5초 / 입력 13,076, 출력 5,404토큰으로 성공했다. 확인한 개선 6개와 남은 제안 1개가 표시됐다. 하단 안내에 대한 남은 제안은 사람의 업무 맥락 판단이 필요하다.
- 최종 단일 검수 재실행 3종 모두 HTTP 200: 승인 목록 68.5초 (입력 9,410 / 출력 3,943), 점검 등록 59.1초 (9,185 / 3,660), 현황 대시보드 59.3초 (9,239 / 3,308). 저장 예시를 해당 실제 결과로 갱신했다.
- HTML·Markdown·JSON 내보내기에서 재검토 근거와 불확실 판정, 담당자 판단이 유지되도록 수정했다. HTML에는 외부 문자열 이스케이프와 스크립트 차단 정책을 적용한다.
- Node 테스트 18개, 제출 로그 비밀정보 제거 테스트 2개, 프로덕션 빌드 통과. 세션·API 키를 출력하거나 커밋하지 않았다.
- 로컬 서버 재시작 중 브라우저가 연결 거부 오류 페이지에 머물렀다. HTTP health 200을 확인한 후 새 검증 탭에서 정상 화면에 재연결했다. 방화벽이나 보안 설정은 변경하지 않았다.
- 데스크톱 결과 화면의 텍스트 109개 부분 대비 검사에서 남은 안내 문구 1개(4.42:1)를 발견하고 색상을 조정했다. 390px 모바일 결과 화면은 문서 너비 375px, 내용 너비 375px로 가로 넘침 없음.

## 공개 재검증과 형식 안정성 보강

- 커밋 106f384의 GitHub Actions 실행 35588467387은 성공했고, Vercel 운영 배포 Ready를 확인했다.
- 공개 페이지에서 점검 등록 화면을 실제 분석했다. 58.9초 후 5개 관찰과 AI 근거 재검토 표시를 확인했고, 담당자 채택 선택과 보고서 미리보기가 정상 동작했다.
- 같은 공개 버전의 전후 비교에서 invalid_response가 다시 발생했다. 실패를 기록하고 성공으로 표시하지 않았다. 실패한 이전 응답의 원문을 보관하지 않았으므로 정확한 필드 원인을 단정하지 않는다.
- 후속으로 검증 실패 유형만 출력하는 진단을 추가했다. 이미지·목적·API 키·원본 모델 응답은 진단 로그에 출력하지 않는다. 비엄격 모드 진단 재실행은 성공하여 해당 실패를 재현하지 못했다.
- 공식 문서(https://platform.claude.com/docs/en/agents-and-tools/tool-use/strict-tool-use, https://platform.claude.com/docs/en/build-with-claude/structured-outputs)를 확인해 strict:true 도구 모드와 지원 스키마 범위를 적용했다. 배열 최대 개수 등 미지원 제약은 설명에 남기고 서버 검증을 유지한다. 단일/비교 모드에 맞게 상태 enum도 제한한다.
- strict 모드 실제 전후 비교 3종 통과: 승인 103.1초 (입력 12,882 / 출력 4,665), 점검 78.3초 (12,564 / 4,067), 대시보드 64.5초 (12,031 / 3,457). 이 결과는 제한된 세 사례의 성공이며 모든 입력의 오류가 제거되었다는 보증은 아니다.
- 불확실한 클릭 후 동작은 uncertain으로 남았다. 일부 AI 요약에 업무 효율 향상을 단정하는 표현이 남을 수 있으며 실제 사용자 성과를 측정한 것은 아니다. 최종 발표에서 성능이나 효과 수치로 인용하지 않는다.
- 사용자 개선 후 이미지를 교체했는데도 저장 비교 예시 버튼이 남을 수 있는 상태 불일치를 발견했다. 두 화면과 목적이 모두 원래 예제일 때만 저장 예시를 허용하도록 수정하고 회귀 테스트를 추가했다.
- 분석 오류가 입력 패널에만 표시되어 모바일 결과 영역에서 놓칠 수 있어 결과 영역에도 오류 설명·재시도·명시적 저장 예시 선택을 추가했다. 로컬 연결 실패 → 재시도 → 실패 상태 표시 → 저장 예시 선택 → 출처 표시 결과 복구를 브라우저에서 확인했다. 취소 버튼은 로컬 오류가 먼저 발생해 해당 시도에서 검증하지 못했다.
- 실제 공개 앱 화면을 브라우저 도구로 캡처했다. 영상은 화면을 단계별로 이어 붙이고 대기 시간을 생략한 편집 영상으로 준비한다. 세션 로그에서 이미지 블록만 지정 시각으로 추출하며 텍스트·키는 영상 작업 폴더로 복사하지 않는다. 도구의 image/png 표기와 실제 JPEG 바이트가 달라 바이트 형식으로 구분했다.
- 영상 인코딩용 imageio-ffmpeg 0.6.0을 artifacts/private/media-tools에만 설치했다. 시스템 환경이나 공개 앱 의존성은 변경하지 않았다. 발표 스킬의 전용 런타임 로더가 현재 도구 목록에 없어 웹 발표자료를 우선 준비하고, PPTX는 검증 가능한 생성 환경을 확보한 경우에 추가한다.

## 목표 해석 정정 — 2026-09-21 19:52 KST
사용자가 이 세션에서 “사이트 url을 제공했을 때 해당 사이트의 ui ux 적인 측면에서 수정해야할 점이 있을지를 확인”하는 도구라고 명확히 정정했다. 기존 에이전트는 스크린샷 업로드 중심으로 잘못 해석했다. 최초 /goal 원문은 제출 증거로 그대로 보존한다. 이후 실행의 우선 완료 상태는 공개 사이트 URL 입력 → 실제 페이지 렌더링/화면 및 구조 관찰 → UI·UX 수정 제안과 근거 보고서이다. 기존 스크린샷 분석은 내부 분석 및 보조 입력에 재사용한다. 기존 방향의 영상 제작은 중단하고 URL 흐름 구현 후 새로 만든다.

첫 구현 범위는 인증 없이 접근 가능한 HTTP(S) 공개 페이지 1개이다. 사용자가 선택한 URL을 실제로 열어 관찰한 범위를 표시하고, 읽기만 수행하며 로그인·폼 제출·사이트 수정은 하지 않는다. 주소 유효성, 내부망 접근 차단, 리다이렉트, 로딩 실패, 데스크톱/모바일 관찰, 실제 URL 분석과 보고서 연결을 검증한다. 로그인 또는 사내망 접근 지원은 자동으로 약속하지 않는다. 기존 완료 확인 항목 가운데 URL 중심 목적과 충돌하는 부분은 이 사용자 정정에 따라 대체하고, 로그에 실제 검증 결과를 남긴다.

사용자 정정은 최초 /goal의 ‘임의 URL 크롤링 제외’와 ‘화면 캡처 중심’ 해석을 대체한다. 공개 URL 한 페이지를 사용자가 명시적으로 지정해 읽기 전용으로 관찰하는 기능은 이제 핵심 범위다. 자동 로그인·내부망·사이트 수정 금지는 유지한다. 19:50 실행한 기존 공개 비교 회귀 검증은 69.7초에 성공했으나, 이것만으로 정정된 URL 검사 목표를 완료했다고 보지 않는다.

## URL 중심 구현 첫 실행 — 2026-09-21 20시대 KST
- `puppeteer-core@25.11.0`, `@sparticuz/chromium@153.0.0`, `ipaddr.js@2.5.0` 설치. npm audit에서 알려진 취약점 0개.
- 공개 IP만 연결하는 per-capture 프록시를 구현했다. DNS 응답 전체를 확인하고 검증한 IP로 직접 연결하여 DNS 재해석을 피한다. 브라우저의 로컬 주소 프록시 우회도 끄고 GET/HEAD 이외 요청을 차단한다.
- URL 기본 입력, 실제 페이지 캡처 API, 데스크톱/모바일 관찰용 `site` 분석 모드를 연결했다. `after`는 이 모드에서 모바일 이미지 식별자이며 개선 전후 의미가 아님을 프롬프트와 UI에 명시했다.
- 로컬 프로덕션 서버 3002에서 공개 데모 URL을 입력하고 직접 실행했다. 실제 페이지가 캡처되어 미리보기에 나타났고 Claude 분석+근거 재검토가 84.9초에 성공했다. 모바일 예제 설명 생략 등 4개 제안이 반환됐다. 이 결과는 해당 시점에 배포돼 있던 스크린샷 중심 앱에 대한 검사다.
- 현재 관찰은 페이지 1개와 첫 화면 두 크기이다. 전체 사이트를 순회하거나 버튼 클릭을 검증했다고 주장하지 않는다.
- 최초 빌드 및 자동 테스트 22개 통과. 이후 URL 메타데이터/보고서 검증을 추가하여 다시 검사 중이다. 공개 Vercel의 Chromium 실행은 아직 확인 전이다.

## 지속 개선 지시 및 한글 렌더링 수정
사용자는 다음날 오전 10시까지 세션을 계속 진행하면서 결과물을 피드백·수정하고 실제 사이트로 반복 검증하라고 명시했다. 이 지시는 현재 활성 /goal의 개발→실행→검증→수정 루프를 강화한다. 오전 9시부터 안정화하고 10시 인계하는 일정은 유지한다.

첫 URL 기능 릴리스 8ae2d70은 CI 및 Vercel 배포가 성공했다. 공개 URL 검사에서 페이지 열기와 캡처가 성공했으나, 확대 화면을 직접 확인하니 서버 캡처의 한글 글자가 누락되었다. Linux Chromium에 한글 글꼴이 없는 것이 원인이다. 해당 실행의 AI 결과는 정상 화면 검수 근거로 채택하지 않는다. Google Fonts 공식 Noto Sans KR 원본과 OFL 라이선스를 포함해 서버 폰트 디렉터리에 로드하도록 수정 중이다. 또한 Chromium 기본 인수의 불필요한 web-security 비활성화 및 mixed-content 허용을 제거하고, 자식 브라우저 프로세스에 KEY/TOKEN/SECRET/PASSWORD/CREDENTIAL 환경변수를 전달하지 않도록 했다.

## 20:18 KST 공개 URL 재검증 피드백

글꼴 수정 배포 4a5ca4b에서 한글이 정상적으로 보이는 캡처를 브라우저 확대 화면으로 직접 확인했다. 공개 URL→캡처→Claude 검수 및 재검토가 성공했고 분석 소요 시간은 67.0초였다.

결과 품질에서는 개선점이 발견됐다. ‘버튼이 첫 화면 아래에 있다’와 ‘동의/버튼이 첫 화면 아래에 있다’가 중복 지적됐고, 화면 밖 요소에 근사 위치가 붙었다. 스크롤이 필요하다는 것 자체는 결함이 아니므로 첫 화면 밖 요소의 추정 좌표와 단순 스크롤 필요를 문제 목록에서 제외하고, 중복 원인은 하나로 합치도록 생성·재검토 지침을 강화했다. 불확실 판정 항목의 마커는 표시하지 않고 목록에 ‘추가 확인 필요’를 드러내도록 수정 중이다. 실제 GS건설 공개 홈페이지도 도구에 입력해 다음 검증을 시작했다.

GS건설 홈페이지 https://www.gsenc.com/ 실제 검사는 캡처 단계에서 HTTP 422 `capture_failed`를 반환했다. 별도 일반 브라우저로 대조한 결과 해당 시점의 페이지가 `/error.png?aspxerrorpath=/Main.aspx`로 이동해 “Sorry, the page not found...” 이미지를 표시했다. 따라서 이 실행을 앱의 성공 분석이나 사이트 UI 결함 발견으로 계산하지 않는다. 접근 가능한 다른 공개 사이트(GS 소개 페이지)로 검증을 이어간다.

GS 소개 페이지의 첫 HTTP 테스트는 사이트 자체의 HTTP 오류를 받기 전에 로컬 테스트 클라이언트에서 `ECONNRESET`으로 중단됐다. 사이트 결함 또는 앱 성공으로 분류하지 않고 브라우저 입력 흐름으로 재검증한다. 재현용 HTTP 테스트 스크립트와 내부 HTTP/CONNECT 차단 통합 테스트를 추가했다. 프록시 차단 3개 테스트 및 전체 빌드가 통과했다.

## 스크롤 구간 구현 및 실제 사이트 재검증
이전 목표 턴은 진행 상태였다. URL 기본 흐름, Linux 한글 렌더링, 피드백 기준 수정이 실제 커밋·배포로 반영됐다. 이번 턴에서는 실제 GS 소개 페이지의 원본과 AI 지적을 대조하여 관찰 오류를 찾아 수정했고, 검사 범위를 최대 6개 캡처 구간으로 넓혔다.

GS 소개 페이지를 로컬 프로덕션 서버의 실제 API로 다시 검사했다. 캡처는 18.557초에 성공했고, 데스크톱 스크롤 위치 0/2495/4990px 및 모바일 0/3828/7656px의 총 6개 이미지를 확보했다. 중간·하단 이미지를 직접 열어 실제 내용과 한글 표시를 확인했다. 전체 요청은 131.878초, AI 입력 29,391 / 출력 3,870 토큰으로 성공했다. 최초 지적 5개 중 근거 재검토에서 2개를 제외했다. 검은 글자를 흰색이라고 설명하던 이전 관찰 오류는 이번 결과에 없었다. 다만 장식 이미지 중첩 지적은 낮은 우선순위로 남아 추가 품질 검토 대상으로 기록한다.

자동 테스트 26개와 빌드가 통과했다. UI의 구간 선택 및 공개 배포 검증은 다음 단계이며, 로컬 API 성공만으로 전체 검증 완료라고 하지 않는다.

## 공개 6개 구간 및 보고서 전달 검증
f3b4086의 GitHub CI(35595017428)와 Vercel 배포가 성공했다. 공개 UI에서 GS 채용 페이지 https://www.gs.co.kr/ko/career 를 입력해 6개 구간을 캡처하고 실제 분석이 63.0초에 완료됨을 확인했다. 결과 4개 중 불확실한 지적 1개에는 ‘추가 확인 필요’가 표시되었고 해당 항목의 마커는 보이지 않았다. 중간·마지막 구간을 참조하는 지적이 실제로 반환되어 첫 화면 이외 관찰이 전달되는 것을 확인했다.

실제 GS 소개 페이지 분석 결과에 검증용 ‘추가 확인’ 판단을 넣어 공개 /api/export에 JSON·Markdown·HTML 저장을 요청했다. 세 형식 모두 HTTP 200과 attachment 헤더를 반환했고, 검사 URL·6개 구간 범위·담당자 판단·관찰 개수가 보존됐다. 파일은 artifacts/private/site-exports에 보관했다.

브라우저 확대 기능에서는 두 가지 오류를 관찰했다. 분석 중 모바일 마지막 구간을 보고 있었는데 분석 완료 시 첫 지적의 데스크톱 화면으로 바뀌었다. 또한 ‘화면에 맞춤’이 너비만 맞춰 모바일 캡처를 과도하게 확대했다. 사용자가 고른 구간을 보존하고 높이까지 맞추는 수정을 적용 중이며, 동작 확인 전에는 검증 완료로 표시하지 않는다.

## 21시대 오탐 비교 및 대기 모드 중단 기록
Example Domain의 실제 공개 페이지를 검사한 첫 UI 실행에서는 정상적인 본문-링크 간격을 과도한 여백으로 지적했다. 별도 동일 캡처/동일 초안 실험에서도 Sonnet 4.6 재검토는 4개 중 1개만, Opus 4.6 재검토는 2개만 제외했다. 두 모델 모두 '관찰 사실이 참'인 것과 '고칠 필요가 있는 문제'를 혼동하는 사례가 있었다. 모델 교체는 채택하지 않고, 사용자 목적에 구체적인 장애가 있어야 supported로 남기도록 생성·재검토 기준을 강화했다. 같은 초안을 다시 검토한 결과 두 모델 모두 4개를 제외했다. 이는 한 가지 음성 대조 사례의 결과이며 전반적인 정확도 수치로 주장하지 않는다. 캡처와 각 실험 결과는 artifacts/private/sites/1789991769687에 보존했다.

입력 폼 개선 전후 실제 API 회귀는 33.309초에 성공했고, 레이블/필수 정보 보완이 resolved로 유지됐다. 승인 요청 화면의 동시 회귀는 실행 도중 Windows 대기 모드로 중단되어 502 upstream_unavailable로 끝났다. 기록상 경과 17,025,026ms에는 대기 시간이 포함되어 정상적인 분석 시간이나 성공 검증으로 계산하지 않는다.

Windows Kernel-Power 이벤트에서 2026-09-21 21:02:30 대기 모드 진입과 2026-09-22 01:45:27 종료를 확인했다. 이 구간에는 연속적인 개발/검증이 이루어졌다고 주장하지 않는다. 01:47부터 오전 10시에 자동 해제되는 SetThreadExecutionState(ES_CONTINUOUS | ES_SYSTEM_REQUIRED) 요청을 별도 숨김 프로세스로 적용했다. 요청 함수의 성공과 프로세스 생존을 확인했다. 전원 설정을 영구 변경하지 않으며, 명시적인 덮개 닫기/전원 버튼 절전은 막지 못한다. 중단된 승인 검증은 별도로 재실행한다.

UI에서는 분석 중 선택한 모바일 구간을 완료 후에도 보존함을 Example Domain 실제 실행으로 확인했다. 확대 맞춤은 고정 높이 추정 대신 컨테이너의 실제 가용 높이에 비례하도록 수정했고 1280×720 가로 화면에서 잘림이 없음을 확인했다. 세로 화면은 추가 확인한다. DOM 관찰값에는 실제 요소의 폭·높이·글자 크기를 더했고, 18,000자 한도를 넘을 때도 뒤쪽 모바일 구간이 잘리지 않도록 모든 구간에서 균형 있게 항목을 줄여 유효한 JSON을 유지한다. 관련 자동 테스트 포함 27개가 통과했다.

## 2026-09-22 08:54 KST 재개와 안정화
08:54에 현재 상태를 다시 확인했다. 01:47 시작한 임시 절전 방지 프로세스는 더 이상 존재하지 않았으며 종료 로그도 없었다. 01시대 이후의 공백에 실제 개발·검증을 수행했다고 주장하지 않는다. 두 번째 중단의 원인은 현재 기록만으로 확정하지 않는다. 오전 9시부터는 원래 목표에 따라 새 대형 기능을 추가하지 않고 현재 URL 검사 안정화, 배포, 영상·발표자료·로그 준비에 집중한다. 제안했던 합성 HTML 업무 페이지는 아직 구현하지 않았으며 기존 이미지 예제와 혼동하지 않는다.

승인 화면의 재실행은 52.498초에 HTTP 200으로 성공했다. 색상 점만 있는 상태, 검색 대상 안내, 일자 컬럼의 의미 등 3개 지적이 유지됐다. 이 양성 대조와 Example Domain 음성 대조를 근거로 기본 모델은 Sonnet 4.6을 유지하고 개선한 프롬프트를 배포한다.

## GS건설 URL 실패 원인과 수정 — 09시대 KST
사용자가 https://www.gsenc.com 검수 실패를 보고했다. 현재 일반 브라우저에서는 정상적으로 열리므로 어젯밤 원본 사이트 오류와 같은 원인으로 단정하지 않았다. 실제 공개 API는 400 url_not_public을 반환했다. 동일 캡처 코드를 로컬에서 진단한 결과 전체 요청 154개 중 34개가 누적 120개 한도에 걸렸고, 모바일 전환 과정의 주 문서 재요청도 1개 차단되었다. 최종 주소가 chrome-error:가 되면서 공개 URL 검사 오류로 잘못 분류된 것이 직접 원인이다.

요청 한도를 화면 크기별 180개, 전체 360개로 나누고 모바일 전환 시 해당 화면 한도만 새로 시작하도록 수정했다. 기존 공개 IP/DNS 검증, HTTP(S) 제한, 읽기 전용 요청, 45MB 전송 한도는 유지했다. 문서 로딩 실패와 리소스 한도 오류도 공개 주소 오류와 구분했다. 실제 GS건설 로컬 재검증은 HTTP 200, 22.492초에 성공했고 데스크톱 3개·모바일 1개 총 4개 캡처를 반환했다. 모바일 캡처 이미지를 직접 열어 건물 이미지, 메뉴, 채용/스토리 영역, 하단 링크가 정상적으로 보이는 것을 확인했다. 공개 배포의 전체 AI 분석은 후속 검증한다.


## GS건설 공개 서버의 추가 원인 — 09:29 KST
09:25:15 공개 서버 로그에서 ERR_EMPTY_RESPONSE와 proxy.bytes=45,052,168을 확인했다. 요청 개수 제한 수정 후에도 전체 45MB 전송 한도가 모바일 로딩 중 연결을 종료시켰다. 화면 크기별 45MB, 전체 90MB로 예산을 분리하고 한도 도달 시 이후 새 연결도 거부하도록 수정한다. 공개 IP/DNS 고정, 인증서 검증과 읽기 전용 요청은 유지한다. 이 수정의 공개 재검증 결과는 후속 기록한다.

GS 채용 분석에서 캡처 경계에 걸린 텍스트를 실제 UI 잘림으로 지적하는 과잉 관찰이 남아 있었다. 이미지 경계와 컨테이너 내부 잘림을 구분하고, DOM emptyButtons를 실제 접근성 이름 부재로 확정하지 않도록 생성·재검토 지침을 보완했다.

## 영상 및 공개 저장소 확인 — 09:33 KST
공개 /demo 페이지의 영상이 재생 시작 후 currentTime=60, duration=60, ended=true, error=null로 끝났음을 브라우저에서 확인했다. 30초 지점으로 키보드 탐색하여 모바일 확대 장면도 확인했다. GitHub 새 탭에 Sign in과 Public 표시가 있고 소스 파일·README·공개 링크를 로그인 없이 읽을 수 있었다. 별도 시크릿 모드 창을 생성한 검증은 아니므로 제출 전 사용자 확인 항목으로 남긴다.


09:43:50 공개 진단에서 GS건설 단일 JPEG 11,446,044바이트, PNG 6,456,068바이트 등 상위 이미지 6개만 약 31.9MB임을 확인했다. 데스크톱 자체가 45MB를 넘어서 화면별 분리만으로는 충분하지 않았다. 이 측정에 근거해 화면별 90MB, 전체 180MB로 제한을 조정한다. 여전히 정해진 요청·전송량·시간 상한을 적용하고 한도 초과 시 실패 처리한다. HTML/스크립트의 임의 변경이나 인증서 검증 해제는 하지 않는다. 성공 시 실제 수집 시간·전송량만 로그로 남겨 재검증한다.


GS건설 공개 캡처는 수정 후 39.087초에 성공했다. 다만 서버 브라우저 기본 언어 때문에 최종 주소가 /en/이고 영문 메뉴를 관찰하는 차이가 있었다. 한국어로 사용하는 도구의 재현성을 위해 HTTP와 브라우저 언어를 ko-KR 우선으로 고정했다. 사이트 자체의 다른 언어/지역 리다이렉트는 최종 URL을 그대로 표시하며 검증하지 않은 국문 결과라고 가장하지 않는다.

GS건설 공개 HTTP 전체 검사는 143.514초에 성공해 지적 2개, API 입력 32,309/출력 3,918토큰을 반환했다. 브라우저에서 목적을 채용 탐색으로 구체화한 별도 실행은 region_bounds로 거부됐다. 잘못된 위치 사각형 하나 때문에 전체 결과를 잃는 문제였다. 좌표를 추측해 보정하지 않고 해당 항목만 제외한 뒤 한계에 제외 개수를 공개하고 남은 항목을 재검토하도록 수정했다. 검증기 자체의 범위 검증은 유지한다.
